upstream apts_backends {
  server unix:/srv/sites/apts/run/gunicorn.sock fail_timeout=0;
}

server {
    listen 80;
    server_name apts.org;

    client_max_body_size 4G;

    access_log /srv/log/apts/nginx_access.log;
    error_log /srv/log/apts/nginx_error.log;

    location /static/ {
        alias   /srv/sites/apts/static/;
    }

    location /media/ {
        alias /srv/sites/apts/media/;
    }

    location / {
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $http_host;
        proxy_redirect off;

        if (!-f $request_filename) {
            proxy_pass http://apts_backends;
            break;
        }
    }

    error_page 500 502 503 504 /500.html;
    location = /500.html {
        root /srv/sites/apts/static/;
    }
}